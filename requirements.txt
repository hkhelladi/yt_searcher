google-api-python-client==2.118.0
google-auth==2.28.0
pyyaml==6.0.1
python-dotenv==1.0.1

# Enrichment pipeline (M2..M9). Required when run_config has enrichment.enabled: true.
httpx==0.27.2
isodate==0.6.1

# Optional: tech detection (M4). Only needed when run_config has
# enrichment.tech_detect: true. Install with `pip install python-Wappalyzer`.
# python-Wappalyzer==0.4.4
